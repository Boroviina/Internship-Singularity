const allLanguages = [
  "en", "de", "es", "fr", "ja", "zh", ""
];

// const languages = Object.keys(allLanguages);
// const languageNames = new Map(Object.entries(allLanguages));

module.exports = {
  allLanguages,
};
